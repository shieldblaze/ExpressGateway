# Plan for REL-2-01 — Doc/code drift remediation (umbrella)
Finding-ref:     REL-2-01 (high, Open)
Files touched:   `RUNBOOK.md`, `DEPLOYMENT.md`, `METRICS.md`, `docs/metrics-conventions.md` (new), `docs/kernel-floor.toml` (new), `scripts/doc-lint.sh` (new), `.github/workflows/doc-lint.yml` (new)
Approach:
This is the umbrella that lands LAST in Round 4 — after every other
fix in this set has merged. It declares the "table of truth" each doc
section must match, then sweeps the three doc files in one PR.

Table of truth (each row maps doc text → source of truth that proves it):

| Doc section | Truth source |
|---|---|
| Drain budget = `[runtime].drain_timeout_ms` (10 s default) | `crates/lb-config/src/runtime.rs` + REL-2-02 plan |
| `KillMode=mixed`, `TimeoutStopSec=12` (drain+2 s slack) | `packaging/expressgateway.service` |
| SIGHUP reload semantics (file-backed config + cert) | REL-2-03 + CODE-2-13 plans |
| `ArcSwap<TlsConfigBundle>` cert rotation, SIGUSR1 trigger | REL-2-03 plan |
| `/livez`, `/readyz`, `/startupz` endpoints + status semantics | REL-2-04 plan |
| Active health-check (passive only — deferred control-plane note) | REL-2-05 plan |
| JSON log lines: schema fields enumerated | REL-2-06 plan |
| `traceparent` propagation + sampler config | REL-2-07 plan |
| Per-listener RED metrics + cardinality budget | REL-2-08 plan |
| `accept_inflight`, `accept_errors_total`, `accept_shed_total` | REL-2-09/10 (code-owned) |
| `xdp_conntrack_full_total`, `xdp_packets_total{action}` | REL-2-12/13 plans |
| Binary path `/usr/local/bin/expressgateway` | REL-2-14 plan |
| Kernel-floor (5.15 / 6.1 / 6.6 matrix) | `docs/kernel-floor.toml` (new, single source) |

Verification mechanism:
1. `docs/metrics-conventions.md` enumerates every metric name + label
   keys (`listener`, `backend`, `route`, `kind`, `action`, `family`).
   `METRICS.md` is regenerated from this file plus a `cargo run -p
   xtask -- dump-metrics` introspection pass over the registered
   prometheus families. Mismatch fails CI.
2. `scripts/doc-lint.sh` greps `RUNBOOK.md` + `DEPLOYMENT.md` for:
   - Every binary path → must equal `/usr/local/bin/expressgateway`
     or `target/release/expressgateway`.
   - Every log-line example → must `grep -rq` in `crates/`.
   - Every metric name → must appear in `METRICS.md`.
   - Every signal mentioned (SIGHUP, SIGTERM, SIGUSR1) → must have
     a handler in `crates/lb/src/main.rs` (`grep SignalKind`).
   CI gate: `.github/workflows/doc-lint.yml`.

Proof:
- `scripts/doc-lint.sh` exits 0 on `main` after all Round 4 PRs land.
- New test `tests/doc_truthfulness.rs::test_metrics_md_matches_registry`
  parses `METRICS.md` and asserts every listed family exists in
  `lb_observability::PROM_REGISTRY`.

Risk / blast radius:
Documentation-only PR; cannot break runtime. Only risk is doc-lint
false positives blocking unrelated PRs — mitigate with an `--exempt`
allowlist for context-dependent strings (e.g., illustrative log
examples in the security section).

Cross-ref:    Closes REL-2-01 by absorbing the doc surface for
REL-2-02/03/04/05/06/07/08/12/13/14. Coordinates with code on
CODE-2-15, with proto on PROTO-2-09 doc copy.
Owner:        rel
Lead-approval: approved 2026-05-13 team-lead
