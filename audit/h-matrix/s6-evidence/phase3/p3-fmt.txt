Diff in /home/ubuntu/Code/ExpressGateway/.claude/worktrees/phase3-gate/crates/lb-io/src/http2_pool.rs:72:
 /// itself satisfies that bound, so every pre-existing caller adapts
 /// with a single `.map_err(Into::into)` (or unchanged when its body is
 /// `Infallible`/already-boxed).
[31m-pub type H2ReqBody =
(B[m[31m-    BoxBody<Bytes, Box<dyn std::error::Error + Send + Sync>>;
(B[m[32m+pub type H2ReqBody = BoxBody<Bytes, Box<dyn std::error::Error + Send + Sync>>;
(B[m 
 /// Default H2 max concurrent streams per upstream connection.
 pub const DEFAULT_H2_MAX_CONCURRENT_STREAMS: u32 = 256;
Diff in /home/ubuntu/Code/ExpressGateway/.claude/worktrees/phase3-gate/crates/lb-io/src/http2_pool.rs:266:
         Ok(sender)
     }
 
[31m-    fn take_alive_sender(
(B[m[31m-        &self,
(B[m[31m-        addr: SocketAddr,
(B[m[31m-    ) -> Option<SendRequest<H2ReqBody>> {
(B[m[32m+    fn take_alive_sender(&self, addr: SocketAddr) -> Option<SendRequest<H2ReqBody>> {
(B[m         let mut peers = self.inner.peers.lock();
         match peers.get(&addr) {
             Some(entry) if entry.is_alive() => Some(entry.sender.clone()),
FMT_EXIT=1
