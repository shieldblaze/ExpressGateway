# Plan for ROUND8-L7-14 — Extend proptest harnesses with CVE-class regression seeds; add proptest scaffolding to `lb-l4-xdp`

Finding-ref:   ROUND8-L7-14 (low, status: Open)
Files touched:
  - `crates/lb-h1/tests/proptest_parser.rs`        (extend with "rejects-ill-formed" assertions)
  - `crates/lb-h1/tests/round8_h1_cve_corpus.rs`   (NEW — named regression cases)
  - `crates/lb-h2/tests/proptest_hpack.rs`         (extend with PADDED grammar)
  - `crates/lb-h2/tests/round8_h2_cve_corpus.rs`   (NEW — PADDED + flag-combo cases)
  - `crates/lb-h3/tests/proptest_qpack.rs`         (extend with H3-specific CVE seeds)
  - `crates/lb-l4-xdp/tests/round8_proptest_pkt_parse.rs`  (NEW — cross-cut to div-l4)
  - `xtask/` or `Cargo.toml`                       (CI gate to run with `PROPTEST_CASES=200000` on the corpus tests)

Approach (≤500 words):

References: **hyper GHSA-f3pg-qwvg-p99c** (CL `+` parsed),
**GHSA-5h46-h7hh-c6x9** (chunk overflow), **GHSA-6hfq-h8hq-87mf**
(TE folding); **nginx CVE-2013-2028, CVE-2019-9516**; **HAProxy
CVE-2023-25725** + `BUG/MAJOR: mux_h1: fix stack buffer overflow in
h1_append_chunk_size`. Every reference proxy maintains regression
seeds for *every* parser CVE they paid for. CI replays them.

Our `proptest_parser.rs` checks "no panic" + "bounded consumption"
only. It does *not* assert "rejects ill-formed". This finding is the
meta-shape behind L7-02, L7-03, L7-04, L7-11.

Concrete shape:

**1. `crates/lb-h1/tests/round8_h1_cve_corpus.rs` — named seeds:**

```rust
#[test] fn empty_header_name_rejected() { /* L7-03 */ }
#[test] fn header_name_with_control_char_rejected() { /* L7-03 */ }
#[test] fn header_name_with_underscore_rejected_under_reject_policy() { /* L7-05 */ }
#[test] fn content_length_plus_sign_rejected() { /* L7-02 cousin — CL parser */ }
#[test] fn chunk_size_plus_sign_rejected() { /* L7-02 */ }
#[test] fn chunk_size_overlong_zero_pad_rejected() { /* L7-02 */ }
#[test] fn chunk_size_inner_whitespace_rejected() { /* L7-02 */ }
#[test] fn transfer_encoding_chunked_must_be_last() { /* hyper GHSA-6hfq-h8hq-87mf */ }
#[test] fn obs_fold_rejected_by_default() { /* RFC 9112 §5.2 */ }
#[test] fn host_with_comma_rejected() { /* L7-09 */ }
#[test] fn host_with_whitespace_rejected() { /* L7-09 */ }
#[test] fn authority_with_control_char_rejected() { /* L7-09 */ }
```

Each test is a *named seed* with a docstring citing the CVE/GHSA that
motivated it. Failure of any of them is a regression.

**2. `crates/lb-h2/tests/round8_h2_cve_corpus.rs`:**

```rust
#[test] fn data_padded_strips_padding() { /* L7-11 */ }
#[test] fn data_padded_pad_length_exceeds_payload_errors() { /* L7-11 */ }
#[test] fn headers_padded_with_priority_decoded_correctly() { /* L7-11 */ }
#[test] fn rapid_reset_counted_per_pingora_pattern() { /* CVE-2023-44487 */ }
#[test] fn continuation_flood_caps_at_threshold() { /* known h2 abuse */ }
#[test] fn hpack_decompression_ratio_capped() { /* hpack bomb */ }
```

**3. Extend proptest assertions (not just "no panic"):**

In `proptest_parser.rs`, add the "well-formed-input round-trips"
assertion: for any input that conforms to a defined H1 grammar
strategy, parsing succeeds; for any input from the *malformed*
strategy, parsing returns `Err`. `prop_assume!` partitions the input
space; the assertion side becomes meaningful.

**4. CI gate:**

Add an `xtask` target `cve-corpus` that runs:
```
PROPTEST_CASES=200000 cargo test --release \
    -p lb-h1 --test round8_h1_cve_corpus \
    -p lb-h2 --test round8_h2_cve_corpus \
    -p lb-h3 --test proptest_qpack
```
Wire into CI's gate matrix.

**5. Cross-cut to `lb-l4-xdp` (div-l4 cross-cut):**

The lb-l4-xdp crate has no proptest harness today. Add
`tests/round8_proptest_pkt_parse.rs` seeded with malformed packet
shapes mirroring known kernel-XDP CVE classes (Katran lesson 4
new-flow-rate, fragment-handling — cross-ref L4-08, conntrack
LRU eviction shapes — cross-ref L4-02). **div-l7 ships the
scaffolding (proptest dependency + test entry + a smoke seed);
div-l4 owns adding the L4-specific predicates** in their own Phase
D plans.

**Boundary disclosure:** the cross-cut to `lb-l4-xdp/tests/` lives
in div-l4's house. div-l7 supplies the proptest scaffolding pattern
only; the L4-specific test bodies are div-l4's responsibility under
their own ROUND8-L4-* plans.

Proof:
  - `round8_h1_cve_corpus` — all named seeds pass.
  - `round8_h2_cve_corpus` — all named seeds pass.
  - `round8_proptest_pkt_parse::smoke_runs_at_least_one_case` — scaffolding works; div-l4 expands with real predicates.
  - CI gate `cve-corpus` runs at `PROPTEST_CASES=200000` and completes within the gate's time budget (record the wallclock target in the README).

Risk / blast radius:
  - Test-only changes. Risk = false-positive flakes if proptest
    case counts are tuned too high for the CI runner. Mitigate by
    starting at `PROPTEST_CASES=10000` for the named-seed corpus and
    only raising the count after baselining.

Cross-ref:
  - Bundle B-6 in `audit/round-8/cross-review.md §B`.
  - Every other L7-0* fix in this round contributes one or more
    named seeds to the corpus.
  - div-l4's plans for L4-02, L4-03, L4-08 should hook into the
    scaffolding shipped by this plan.

**Audit-failure-mode theme:**
  - **Theme 4 — Multi-validator audit handoff**: CODE-2-11 shipped
    proptest harnesses; "rejects CVE-class inputs" assertions absent.
    The protocol validator was supposed to seed the harness with
    the CVE corpus and never did. This plan closes the meta-failure.
  - **Theme 1 — "Verified-Fixed" snapshot of script existence, not
    capability**: the proptest harness existed at `Verified-Fixed`
    time but its *assertions* were trivially weak. Doc-lint must be
    extended (bundle B-2, div-ops) to read the assertions and
    require named seeds for any closed parser-CVE finding.

Owner:           div-l7 (scaffolding); div-l4 contributes L4-specific predicates.
Lead-approval: approved 2026-05-14 team-lead-r8
