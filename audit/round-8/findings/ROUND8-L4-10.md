### ROUND8-L4-10 — EBPF-2-07 is "Verified-Fixed" but no verifier logs are committed; the diff-gate is a no-op

Reference: `audit/round-8/research/kernel-selftests.md` ("the kernel's own regression tests for the BPF subsystem... assert exactly which error the verifier returned"); handoff Round-7 retrospective
Our equivalent: `scripts/verify-xdp.sh`, `audit/ebpf/verifier-logs/` (only `README.md` present), `audit/ebpf/round-2-review.md:619-624` (EBPF-2-07 row marked Verified-Fixed at `ffde98c`)

Severity: high
Status:   Verified-Fixed (verify, task#70, 2026-05-15) — GATE ACTUALLY FAILS ON DRIFT: verified e2e with a fake docker, real verify-xdp.sh exits 1 on injected drift, exit 2 on missing baseline, exit 0 on identical. No audit-of-audit recursion. Caveat: baselines are intentional fail-closed HARNESS-CAPTURED-PENDING-CI-RERUN placeholders (gate fails-loud until first privileged CI capture — by design). See audit/round-8/verify/l4.md.

Divergence (audit-of-audit):
- The Round-2 EBPF table marks EBPF-2-07 as `Verified-Fixed(ffde98c)`.
- Commit `ffde98c` adds only `scripts/verify-xdp.sh` and `audit/ebpf/verifier-logs/README.md`. **No actual `.log` snapshots are committed**.
- `scripts/verify-xdp.sh:111` checks `if [ -f "${OUT_LOG}.committed" ]; then diff ...`. The committed snapshots don't exist; the conditional is always false; the diff-gate is a permanent no-op.
- The script's IMAGE pins are a "TODO: pin digests" comment with floating tag `${IMAGE_BASE}:${KVER}-main`. Reproducibility is not guaranteed.
- The Docker invocation `docker run --privileged ... bpftool prog load ...` would *load* the program but doesn't actually verify packet steering — it only captures the verifier text output.
- `audit/unsafe-justifications.md:109` states "validated by the kernel verifier on every load and additionally pinned via the verifier-log diff gate in CI" — that's not true. The diff gate is dormant.

Impact:
- A future change to `ebpf/src/main.rs` that produces a kernel-specific verifier complaint (e.g. aya #1562-style scalar/ptr reordering on kernel 5.15) ships without anyone noticing, until a customer on that kernel reports it.
- The audit table is misleading: Round-7 "CONDITIONAL GO" relied in part on the EBPF-2-07 Verified-Fixed grade. The grade is a snapshot of the *script existing*, not of the *verification having happened*.

Reproduction:
- `ls audit/ebpf/verifier-logs/` — only `README.md`.
- `git show ffde98c -- audit/ebpf/verifier-logs/` shows only README.md added.
- Run `scripts/verify-xdp.sh 6.1` locally; observe that with no committed snapshot, the script always exits 0.

Recommendation:
1. Re-classify EBPF-2-07 to `Open` until at least one committed `.log` snapshot per kernel exists (5.15, 6.1, 6.6).
2. Add `audit/ebpf/verifier-logs/{5.15,6.1,6.6}.log.committed` files generated by a single privileged CI run, then make the diff hard-fail on drift.
3. Pin the lvh-images digest, not the floating tag.
4. Add a packet-test-run step (`bpftool prog test-run`) that asserts a synthetic packet is rewritten as expected — *correctness*, not just *loadability*.
5. Update `audit/unsafe-justifications.md:109` to match reality, or land the snapshots first.

This finding is `audit-of-audit`: the previous round graded its own work without checking the artifact landed.
