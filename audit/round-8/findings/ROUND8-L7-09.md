### ROUND8-L7-09 — No authority comma / control-char rejection in any protocol parser (HAProxy `BUG/MAJOR: http: forbid comma in authority` class)

Reference: `audit/round-8/research/haproxy.md` lessons 9 + 10 — `BUG/MAJOR: http: forbid comma character in authority value` + `BUG/MEDIUM: h1: Enforce the authority validation during H1 request parsing` (H1 parser was missing the check that H2/H3 had — protocol-neutral validation requires the validation to actually run on *every* parser). `ref-l7` Top-10 #3.
Our equivalent: `crates/lb-l7/src/sni_authority.rs` (SNI-vs-host only), `crates/lb-l7/src/h2_proxy.rs:1218-1263` (`check_authority_host_agreement` — agreement only, no value sanitisation), `crates/lb-h1/src/parse.rs` (no authority validation at all)

Severity: medium
Status:   Proposed-Fix(div-l7, bf22f01a — push-back closed: validator now WIRED on H1+H2 request paths)   <!-- PUSH-BACK RESPONSE (author=div-l7, sha bf22f01a): crate::authority::validate is now called on BOTH parser paths BEFORE upstream selection — H1 at crates/lb-l7/src/h1_proxy.rs (on the Host header, before the SNI check / picker) and H2 at crates/lb-l7/src/h2_proxy.rs handle_inner (on :authority + Host, before check_authority_host_agreement / picker); reject -> 400. Proof crates/lb-l7/tests/round8_authority_enforced.rs 4/4 PASS: comma/whitespace authority -> 400 BEFORE upstream (closed backend would yield 502 if the validator were skipped — proves on-path), valid authority -> reaches upstream (502). Theme-1 "library shipped no caller" resolved. verify re-checks. See audit/round-8/verify/l7.md. -->
          Proposed-Fix(div-l7, task#75 — re-verify bypass closed: authority::validate hoisted to a single choke point ABOVE the upgrade/CONNECT/gRPC forks)   <!-- RE-VERIFY PUSH-BACK RESPONSE (author=div-l7, task#75): the partial-fix bypass found by verify task#74 is closed. New shared helper crate::authority::validate_request<B>(&Request<B>) validates BOTH uri.authority() (H2 :authority / H1 absolute-form) AND the Host header. It is called as the FIRST statement of H1 handle_inner (crates/lb-l7/src/h1_proxy.rs:766-779, above the gRPC-415 reject and the WS-upgrade fork at ~803) and the FIRST statement of H2 handle_inner (crates/lb-l7/src/h2_proxy.rs:642-665, above the extended-CONNECT fork and the gRPC fork). The redundant in-path L7-09 blocks (old h1 ~880 / h2 ~764) were removed — single choke point, less rot-prone than 3 duplicated call sites. No loopback exemption on the choke point (the loopback carve-out applies only to the SNI-vs-Host *agreement* check, NOT to authority value sanitisation) so the upgrade path is neither stricter nor looser than the plain path. Proof crates/lb-l7/tests/round8_authority_enforced.rs now 7/7 PASS: the 4 original cases PLUS test_ws_upgrade_comma_authority_rejected (H1 Upgrade: websocket + comma Host -> 400, real probe backend 0 connections), test_h2_ext_connect_comma_authority_rejected (H2 CONNECT + :protocol websocket + comma :authority -> 400, 0 connections), test_h2_grpc_comma_authority_rejected (H2 content-type: application/grpc + comma :authority -> 400, 0 connections). Each new test uses a REAL accept-counting probe backend and asserts ZERO upstream connections — the bypass is provably gone, not merely moved. verify re-checks (not self-closed). See audit/round-8/verify/fixback.md. -->
          VERIFIED-FIXED (verify, task#76, 2026-05-15, sha 1a89a4e4) — H1/H2 scope. NON-BLOCKING for prod (future-routing primitive). <!-- RE-RE-VERIFY (author=verify, task#76, sha 1a89a4e4, NOT self-closed — div-l7 authored 1a89a4e4): crate::authority::validate_request<B> is the FIRST statement of H1Proxy::handle_inner (crates/lb-l7/src/h1_proxy.rs:779, above the gRPC-415 reject + WS-upgrade fork) and the FIRST statement of H2Proxy::handle_inner (crates/lb-l7/src/h2_proxy.rs:654, above the RFC-8441 extended-CONNECT intercept at 672 and the gRPC fork at 679). handle() in both files only opens the trace span then calls handle_inner. OLD redundant in-path L7-09 blocks REMOVED in both files (no double-validation, no dead path). round8_authority_enforced 7/7 PASS — the 3 new tests (test_ws_upgrade_comma_authority_rejected, test_h2_ext_connect_comma_authority_rejected, test_h2_grpc_comma_authority_rejected) drive REAL RFC-6455 / extended-CONNECT / application-grpc request shapes (not renamed plain GETs) and each asserts a REAL accept-counting probe backend recorded ZERO upstream connections. Adversarial 4th-path hunt: H2->H1 downgrade bridge, non-extended CONNECT, and pipelined kept-alive H1 are all BELOW the choke point — no 4th bypass on the H1/H2 path. H3/QUIC is a SEPARATE unguarded dispatch in the lb-quic crate (conn_actor.rs:361 -> upstream selection, no authority::validate; lb-quic does not depend on lb-l7) — that is OUT OF L7-09's H1/H2 scope and is tracked as the NEW finding ROUND8-L7-16 (the H3 leg this finding's Recommendation step 2 always called for). round8_drain_15case 16/16 (unchanged). See audit/round-8/verify/fixback.md. --> [SUPERSEDED PARTIAL — VERIFIED-FIXED on the normal request path, PUSH-BACK on the upgrade/gRPC paths (verify, task#74, 2026-05-15, sha bf22f01a). round8_authority_enforced 4/4 PASS; the H1 and H2 *normal* request paths DO call crate::authority::validate before pick_info()/upstream selection and reject -> 400 (confirmed at h1_proxy.rs:880 and h2_proxy.rs:764). ADVERSARIAL PROBE FOUND A REAL BYPASS: (1) H1 WebSocket upgrade — h1_proxy.rs:803-809 returns self.handle_ws_upgrade(req) BEFORE the L7-09 validator at line 880; handle_ws_upgrade (line 1235) calls self.picker.pick_info() at 1246 and dials upstream at 1276 with NO authority::validate. (2) H2 extended-CONNECT WS — h2_proxy.rs:645-651 returns handle_ws_extended_connect(req) before the validator at 764; that handler picks upstream at line 954 with no validation. (3) H2 gRPC — h2_proxy.rs:652-672 reaches Arc::clone(gp).handle(req, backend.addr) (upstream) at line 671 before the validator at 764. A WS-upgrade or gRPC request with a comma/whitespace/control byte in Host (H1) or :authority (H2) reaches upstream selection unvalidated. The proof test exercises ONLY plain GET requests — it does not cover any upgrade or gRPC path, so the 4/4 PASS does not prove the bypass closed. Severity is medium and the finding itself notes no host-based routing exists today (the comma is a future-routing primitive, not directly exploitable now), so NON-BLOCKING for prod but the push-back stands: the validator must be hoisted ABOVE the WS-upgrade / gRPC intercepts (or duplicated into handle_ws_upgrade / handle_ws_extended_connect / the gRPC arm) for the "every parser path" claim to hold. See audit/round-8/verify/fixback.md.]

Divergence:
- **Reference**: every protocol parser (H1, H2, H3) must reject `:authority` / `Host` values containing comma, whitespace, control characters, or that are empty. Same predicate, applied uniformly.
- **Us**:
  - H1: `parse_request_line` accepts whatever `Uri::parse` accepts (which is permissive). `Host:` header in `parse_headers` runs through the generic header-parser — no comma or control-char check.
  - H2: `check_authority_host_agreement` only compares `:authority` ↔ `Host`. It does NOT reject `Host: victim.example,attacker.example` if the same value appears in `:authority`.
  - H3: same `:authority`-via-quiche path; no validator wired.

Impact:
- The HAProxy fix exists because downstream intermediaries split on comma and bypass routing decisions. Our `sni_authority::check_sni_authority` strips port and compares case-insensitively — `Host: example.test,attacker.example` after `split_host_port` returns `(example.test,attacker.example, None)` (rsplit_once(':') has no `:` to split on); the comparison then runs against the literal string `"example.test,attacker.example"` vs the SNI `"example.test"` and *correctly* fails — but only because the whole authority is treated as one host literal. The proxy then *routes* on that literal too (the backend picker doesn't see the authority, but if a future change adds host-based routing, the comma will bite).
- Future-proofing: this is a "lesson-not-yet-paid-for" finding. We do not have host-based routing today, so the comma isn't directly exploitable. The day we add it (multi-tenant deployment, vhost routing), the comma becomes a smuggling primitive.

Reproduction:
```rust
let buf = b"GET / HTTP/1.1\r\nHost: example.test,attacker.example\r\n\r\n";
let (_, _, _, n) = lb_h1::parse_request_line(buf).unwrap();
let (h, _) = lb_h1::parse_headers(&buf[n..]).unwrap();
assert!(h.iter().any(|(k, v)| k == "Host" && v.contains(',')));  // currently true — no reject
```

Recommendation:
1. Add `lb_l7::authority::validate(value: &str) -> Result<(), AuthorityError>` enforcing:
   - non-empty after trim
   - no commas
   - no whitespace / tab
   - no control chars (`< 0x20`, `0x7F`)
   - one optional `:port` suffix with digits only
   - bracket-balanced IPv6
2. Call it from:
   - H1: in the H1 request handler before any routing / picker call.
   - H2: inside `check_authority_host_agreement` *before* the agreement compare.
   - H3: in the QUIC conn_actor's header-callback path.
3. Add regression tests with vector `Host: a,b`, `Host: a b`, `Host:` (empty), `Host: \x01host`.
4. Cross-over to `audit/round-8/findings/_ops_summary.md`: when host-based routing lands (any future pillar), this becomes critical.
