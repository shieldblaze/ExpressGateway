# Line Coverage Report

Generated by `cargo llvm-cov --workspace --no-fail-fast --summary-only` at Pillar-4a-plus-Step-5 baseline (HEAD `de5c6dbf`).

## Workspace total

| Metric | Covered | Total | % |
|--------|---------|-------|---|
| **Lines** | 3,384 | 4,485 | **75.45%** |
| **Regions** | 6,841 | 8,012 | **85.38%** |
| **Functions** | 717 | 879 | **81.57%** |

## FINAL_REVIEW §9.2 gate: *"≥80% per crate, exceptions documented"*

Below the 80% line-coverage threshold (exceptions follow):

| File | Lines % | Category | Reason |
|------|--------:|----------|--------|
| `lb/src/main.rs` | 0.00% | Binary entry | Integration-level; no unit-test harness. Covered transitively by `tests/reload_zero_drop.rs` and the manifest-required `controlplane_*` tests, which run the binary end-to-end but don't count against source-file line coverage here. |
| `lb-l4-xdp/src/loader.rs` | 37.10% | Error paths | aya userspace loader. The happy paths (`kernel_load`, `attach`) require CAP_BPF and a real BPF ELF. Only garbage-ELF and empty-bytes rejection paths fire in CI. Raising this requires Pillar 4a follow-up: install `bpf-linker` on a rustc ≥1.88 toolchain, produce `lb_xdp.bin`, add `#[cfg(all(target_os="linux", lb_xdp_elf))]` tests that load it. |
| `lb-grpc/src/status.rs` | 41.30% | Rare branches | Status-code mapping for codes that don't arise in the manifest test suite. Low-priority — numeric decode is simple. |
| `lb-l7/src/h3_to_h1.rs` | 56.82% | Protocol bridge | Each bridge pair has a dedicated test (`tests/bridging_h3_h1.rs`). The uncovered portion is error-handling and trailer propagation paths that the bridging test suite doesn't exercise end-to-end. Pillar 3b H3 interop work will raise. |
| `lb-l7/src/h2_to_h2.rs` | 58.82% | Protocol bridge | Same as above. |
| `lb-l7/src/h2_to_h3.rs` | 58.82% | Protocol bridge | Same. |
| `lb-l7/src/h3_to_h2.rs` | 58.82% | Protocol bridge | Same. |
| `lb-l7/src/h3_to_h3.rs` | 58.82% | Protocol bridge | Same. |
| `lb-l7/src/h1_to_h2.rs` | 57.14% | Protocol bridge | Same. |
| `lb-l7/src/h1_to_h3.rs` | 61.90% | Protocol bridge | Same. |
| `lb-h3/src/frame.rs` | 64.43% | Codec | Frame decode has branches for every wire variant (RFC 9114 §7). The manifest `conformance_h3` test covers headline variants; rare frames uncovered. |
| `lb-h2/src/frame.rs` | 64.16% | Codec | Same as H3 frame — rare SETTINGS identifiers and PUSH_PROMISE paths uncovered. |
| `lb-h2/src/hpack.rs` | 68.52% | Codec | Huffman decoder edge cases uncovered (our encoder intentionally never emits Huffman, per the crate doc). |
| `lb-io/src/sockopts.rs` | 69.37% | Platform | setsockopt branches for `TCP_FASTOPEN`, `TCP_QUICKACK` — exercised only when the `tokio` test runtime and kernel cooperate; non-Linux branches skipped. |
| `lb-io/src/ring.rs` | 71.24% | Platform | io_uring NOP/accept/recv/send/splice ops — each test skips gracefully if kernel rejects; some branches unreachable in this CI kernel. |
| `lb-quic/src/lib.rs` | 75.40% | Network | Real UDP+TLS roundtrip; uncovered branches are connection-error and stream-reset paths that require a fault-injecting peer. |
| `lb-h3/src/qpack.rs` | 76.87% | Codec | Dynamic-table path disabled by design (capacity 0); encoder never exercises it, so decoder acceptance of capacity-0 is the only codepath tested. |
| `lb-security/src/ticket.rs` | 76.00% | TLS | `RotatingTicketer::encrypt` / `decrypt` through `Arc<dyn ProducesTickets>` — rustls-internal paths; our unit tests cover rotation + overlap logic but can't trigger all rustls error returns. |
| `lb-h1/src/parse.rs` | 77.89% | Codec | Borderline; pushes above 80% easily with two more tests for folded-header rejection and obs-fold. |
| `lb-io/src/dns.rs` | 78.88% | Network | Singleflight + background-refresh paths not fully exercised by unit tests; real NXDOMAIN responses depend on system resolver. |

## At or above 80% (no action needed)

All other workspace files, including (selected highlights):

- `lb-core/*`: 100%
- `lb-config/*`: 100% / near
- `lb-controlplane/*`: 100% / near
- `lb-health/src/lib.rs`: 100%
- `lb-observability/src/lib.rs`: 100%
- `lb-grpc/src/lib.rs`: 100%
- `lb-security/{lib,slowloris,zero_rtt}.rs`: 100%
- `lb-h2/src/security.rs`: 91.48%
- `lb-h3/src/security.rs`: 95.45%, `varint.rs`: 91.43%
- `lb-l4-xdp/src/lib.rs` (userspace sim): 90.96%
- `lb-io/src/pool.rs`: 83.33%, `lib.rs`: 83.08%

## How to reproduce

```
cargo install cargo-llvm-cov --version 0.6.21 --locked
rustup component add llvm-tools-preview
cargo llvm-cov --workspace --no-fail-fast --summary-only
```

Newer `cargo-llvm-cov 0.8.x` needs rustc ≥1.87; project MSRV is 1.85 per `rust-toolchain.toml`. When MSRV bumps (see ADR-0003 / Pillar 3b candidate) switch to 0.8.x.

## Commitment

The 80% gate is not mechanically enforced in `scripts/halting-gate.sh`. This file is the honest record. When a listed file's reason resolves (e.g. Pillar 3b lands H2 end-to-end tests), remove its row and note the coverage delta in `CHANGELOG.md`.
